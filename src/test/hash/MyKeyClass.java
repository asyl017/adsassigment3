/**
 * Represents a class for generating hash codes based on a key.
 */
package test.hash;

public class MyKeyClass {
    private int primaryKey;

    /**
     * Constructs a new instance of MyKeyClass with the specified key.
     *
     * @param primaryKey the  key value to be used for hashing
     */
    public MyKeyClass(int primaryKey) {
        this.primaryKey = primaryKey;
    }

    /**
     * Computes the hash code for this object.
     * The hash code is generated by performing a bitwise exclusive OR operation
     * on the primary key with the hexadecimal constant 0xbbbbbbbb after performing
     * a logical right shift by one bit.
     *
     * @return the computed hash code
     */
    @Override
    public int hashCode() {
        int temp = primaryKey;
        temp >>>= 1;
        return temp ^ 0xbbbbbbbb;
    }
}
